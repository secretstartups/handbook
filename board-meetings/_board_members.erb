<% data.team.each do | slug| %>
  <% next unless (slug.locality == "Amsterdam") %>
  <% l = [] %>
##### <%= slug.name %>
<%= slug.name + " " + slug.role + slug.departments.to_s + "  " unless slug.picture.blank? %>
<%= l.join(' • ') unless l.empty? %>
<% end %>